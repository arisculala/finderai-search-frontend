# Usage

```bash
<ContentModal
  open={modalOpen}
  onClose={() => setModalOpen(false)}
  onConfirm={handleDelete}
  title="Add New User"
  confirmText="Save"
  cancelText="Cancel"
>
  Any component....
</ContentModal>
```
